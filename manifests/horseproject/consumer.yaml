apiVersion: apps/v1
kind: Deployment
metadata:
  name: kafka-event-consumer
  namespace: horseproject
  labels:
    app: kafka-event-consumer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kafka-event-consumer
  template:
    metadata:
      labels:
        app: kafka-event-consumer
    spec:
      containers:
        - name: consumer
          image: likespro/horse-backend:latest
          command: ["python", "-m", "app.kafka_event_consumer"]
          env:
            - name: PYTHONPATH
              value: "/app"
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: horse-config
                  key: DB_HOST
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: horse-config
                  key: DB_PORT
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: horse-config
                  key: DB_NAME
            - name: DB_USER
              valueFrom:
                configMapKeyRef:
                  name: horse-config
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: horse-secret
                  key: DB_PASSWORD
            - name: KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: horse-config
                  key: KAFKA_BOOTSTRAP_SERVERS
            - name: KAFKA_TOPIC
              valueFrom:
                configMapKeyRef:
                  name: horse-config
                  key: KAFKA_TOPIC
          resources:
            limits:
              cpu: 500m
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 256Mi
      initContainers:
        - name: wait-for-db
          image: busybox:1.36
          command: ["sh", "-c", "until nc -z db 5432; do echo waiting for postgres; sleep 2; done"]
        - name: wait-for-kafka
          image: busybox:1.36
          command: ["sh", "-c", "until nc -z kafka 9092; do echo waiting for kafka; sleep 2; done"]
